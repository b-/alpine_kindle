name: build_and_release

on:
    push:
        branches: [ master dev ]
    pull_request:
        branches: [ master dev ]

jobs:
    build:
      runs-on: ubuntu-latest
      steps:
      - name: checkout repo
        uses: actions/checkout@main
      - name: build application
        run: just ci
